(use-modules (ice-9 rdelim))
(define input (open-input-file (list-ref (command-line) 1)))
(let loop ((line (read-line input)))
  (cond ((not (eof-object? line))
    (if (even? (string->number line))
      (display 1)
      (display 0))
    (newline)
    (loop (read-line input)))))
(close input)
