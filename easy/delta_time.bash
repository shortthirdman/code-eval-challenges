sed -e "s/:/ /g; s/0\([[:digit:]]\)/\1/g" <$1 | while read line || [ -n "$line" ]; do
  a=( $line )
  t=$(( (${a[0]}-${a[3]})*3600+(${a[1]}-${a[4]})*60+${a[2]}-${a[5]} ))
  t=${t#-}
  printf "%02d:%02d:%02d\n" $(( $t/3600 )) $(( ($t/60)%60 )) $(( $t%60 ))
done
